# roadmaster-landing-page

Site vitrine Roadmaster (Next.js 13, export statique).

```bash
cp .env.example .env.local
npm ci
npm run dev
```

Scripts : `lint`, `test`, `build` (génère `out/`).

Documentation RNCP : `../rncp-dossier-bloc-2/`.
